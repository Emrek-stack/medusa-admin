# Routing and Layout Rules

- Keep route changes within frontend routing files.
- Preserve existing layout shells and navigation structure unless explicitly requested.
- If adding new pages, follow existing route module organization and naming.
- Prefer existing table/form/modal layout patterns already used in `src/routes` and `src/components`.
