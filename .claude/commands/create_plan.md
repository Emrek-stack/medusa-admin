# create_plan (UI-only)

Create an implementation plan only for frontend scope.

## Must include
1. Affected UI files/routes/components
2. Reusable existing patterns in repo
3. State management changes (if any)
4. i18n/theme impact
5. Validation checklist (build + manual UI checks)

## Must exclude
- Backend/API server tasks
- DB/infra tasks
